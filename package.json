{
    "name": "vscode-teal",
    "displayName": "Teal",
    "description": "Teal language support for Visual Studio Code",
    "version": "0.1.0",
    "license": "MIT",
    "repository": {
        "type": "git",
        "url": "git://github.com/pdesaulniers/vscode-teal.git"
    },
    "engines": {
        "vscode": "^1.42.0"
    },
    "categories": [
        "Programming Languages"
    ],
    "main": "./out/client/extension",
    "activationEvents": [
        "onLanguage:tl"
    ],
    "publisher": "pdesaulniers",
    "contributes": {
        "languages": [
            {
                "id": "tl",
                "aliases": [
                    "TL",
                    "tl",
                    "Teal"
                ],
                "extensions": [
                    ".tl"
                ],
                "configuration": "./language-configuration.json"
            }
        ],
        "grammars": [
            {
                "language": "tl",
                "scopeName": "source.tl",
                "path": "./syntaxes/tl.tmLanguage.json"
            }
        ]
    },
    "dependencies": {
        "@types/node": "^13.9.3",
        "@types/tmp": "^0.1.0",
        "tmp-promise": "^2.0.2",
        "vscode-languageclient": "^6.1.3",
        "vscode-languageserver": "^6.1.1",
        "vscode-languageserver-textdocument": "^1.0.1"
    },
    "devDependencies": {
        "@types/vscode": "^1.43.0"
    },
    "scripts": {
        "update-vscode": "node ./node_modules/vscode/bin/install",
        "vscode:prepublish": "cd client && npm run update-vscode && cd .. && npm run compile",
        "compile": "tsc -b",
        "watch": "tsc -b -w"
    }
}
